export const TOKEN_API = process.env.API_TOKEN || '';

export const APP_HOST = `${process.env.NEXT_PUBLIC_APP_HOST}:${process.env.NEXT_PUBLIC_PORT}`;

export const USERS_DB_PATH = 'src/db/user.json';
